# Neuroevent - AI Image Generation Project

Проект Neuroevent представляет собой веб-приложение с интегрированной системой генерации изображений на основе ИИ, плавными анимациями и интерактивным слайдером изображений.

## 🎉 Новая версия на React + Next.js

Проект находится в процессе миграции на **React + Next.js** для решения проблем производительности! 🚀

### ✨ Что уже готово:
- ✅ **React компоненты** созданы (`/components/`)
- ✅ **React демо-страница** (`demo-react.html`)
- ✅ **Улучшенная архитектура** с компонентами
- ✅ **Миграционный гид** (`MIGRATION_GUIDE.md`)

### 🎯 Преимущества новой версии:
- ⚡ **В 2-3 раза быстрее** загрузка и анимации
- 🎨 **Плавные анимации** без конфликтов с системой производительности
- 📱 **Лучшая мобильная поддержка**
- 🔧 **Проще поддержка и развитие кода**
- 🎨 **Современные UI компоненты**

## 🚀 Особенности

- **AI Image Generation**: Интеграция с OpenAI DALL-E 3 для высококачественной генерации изображений
- **Smart Performance Detection**: Продвинутая система детекции производительности устройств
- **Adaptive Animations**: Адаптивные анимации с автоматической оптимизацией под устройство
- **Image Slider**: Автоматический слайдер с зум-анимациями
- **Background Music**: Фоновая музыка "Вселенная Гарри Поттера"
- **Performance Optimized**: Интеллектуальная оптимизация ресурсов без потери визуальных эффектов
- **Responsive Design**: Адаптивный дизайн для всех устройств
- **Unified Server**: Единый Python-сервер для веб-интерфейса и API

## 🎯 Система оптимизации производительности

Проект использует продвинутую систему детекции производительности устройств, которая автоматически адаптирует настройки анимаций:

### Уровни производительности:
- **High** (Высокий): Полный набор анимаций для мощных устройств
- **Medium** (Средний): Оптимизированные анимации для средних устройств
- **Low** (Низкий): Минимальные анимации для слабых устройств

### Метрики оценки:
- Количество ядер процессора
- Объем оперативной памяти
- Качество интернет-соединения
- Поддержка WebGL и уровень GPU
- Уровень заряда батареи
- Размер экрана и тип устройства

### Адаптивные настройки:
- **DALL-E 3**: Качественная генерация изображений с соблюдением контент-политики
- **GSAP анимации**: Скорость, длительность, easing, batch-обработка
- **Slick слайдеры**: Автопрокрутка, скорость, интерактивность, Intersection Observer
- **Particles.js**: Количество частиц, производительность
- **VanillaTilt**: Эффекты наклона карточек
- **Custom Cursor**: Частота обновления, видимость
- **Resource Manager**: Кеширование, lazy loading, предзагрузка по секциям

## 🎭 Интеллектуальная система анимаций

Проект использует продвинутую систему управления анимациями с несколькими оптимизированными менеджерами:

### Smart Slider Manager
- **Intersection Observer** для запуска слайдеров только при видимости
- **Автоматическая пауза** невидимых слайдеров для экономии ресурсов
- **Адаптивные настройки** в зависимости от производительности устройства
- **Обработка ошибок** с автоматическим восстановлением

### GSAP Animation Manager
- **Batch-обработка** анимаций для оптимизации производительности
- **Умное создание таймлайнов** с автоматической оптимизацией
- **ScrollTrigger оптимизация** для разных уровней производительности
- **Автоматическая очистка** устаревших анимаций

### Resource Manager
- **Интеллектуальное кеширование** ресурсов с учетом частоты использования
- **Lazy loading** с приоритетами загрузки
- **Предзагрузка по секциям** для плавного скролла
- **Автоматическая очистка кеша** при нехватке памяти

## 📊 Система мониторинга и адаптации

Проект включает продвинутую систему реального времени мониторинга производительности с автоматической адаптацией:

### Performance Monitor
- **Комплексный сбор метрик** производительности (FPS, память, сеть, батарея)
- **Мониторинг Core Web Vitals** (FID, CLS, LCP)
- **Обнаружение проблем** в реальном времени
- **Исторический анализ** трендов производительности

### Adaptive Manager
- **Автоматическая адаптация** настроек под текущие условия
- **Проактивная оптимизация** на основе предсказаний
- **Реактивная адаптация** на критические ситуации
- **Интеллектуальное управление** частотой адаптаций

### Система обратной связи
- **Взаимодействие менеджеров** для координации изменений
- **Runtime корректировка** настроек без перезагрузки
- **Предотвращение конфликтов** между оптимизациями
- **Отчетность и аналитика** адаптаций

## 📋 Требования

- Python 3.8+
- Node.js (опционально, для разработки)
- Современный веб-браузер
- **OpenAI API ключ** для генерации изображений через DALL-E 3

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/RockInMyHead/nuroweb.git
cd nuroweb
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта (используйте `env-example.txt` как шаблон):

```env
OPENAI_API_KEY=your_openai_api_key_here
PORT=8002
```

**Получение OpenAI API ключа:**
1. Перейдите на [platform.openai.com](https://platform.openai.com)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел "API Keys"
4. Создайте новый API ключ
5. Скопируйте ключ и вставьте в переменную `OPENAI_API_KEY`

**Важно:** DALL-E 3 требует оплаты. Проверьте [тарифы OpenAI](https://openai.com/api/pricing/) перед использованием.

### 4. Запуск приложения

```bash
python3 server.py
```

### 5. Открытие приложения

## 🌐 Доступ к приложению

### Текущая версия (Vanilla JS):
```bash
python3 server.py
# Откройте: http://localhost:8002/demo
```

### 🆕 Новая версия (React + Next.js):
```bash
# React демо через CDN
python -m http.server 3000
# Откройте: http://localhost:3000/demo-react.html

# Полная Next.js версия (в разработке)
# npm run dev
# Откройте: http://localhost:3000
```

### Production версия:
- **Основная страница**: https://neuroevent.ru/
- **Демо страница**: https://neuroevent.ru/demo
- **API endpoints**: https://neuroevent.ru/api/

### 6. Проверка оптимизации

Для проверки работы системы оптимизации откройте страницу тестирования производительности. Система автоматически:
- Определит характеристики вашего устройства
- Назначит оптимальный уровень производительности
- Применит соответствующие настройки анимаций
- **Запустит мониторинг в реальном времени**
- **Выполнит автоматическую адаптацию при необходимости**

#### Доступные страницы тестирования:
- **Базовое тестирование**: `https://neuroevent.ru/performance-test.html`
- **Главная страница**: `https://neuroevent.ru/` (с полной системой оптимизации)

#### Возможности тестирования:
- **Метрики в реальном времени**: FPS, память, сеть, батарея
- **Статистика менеджеров**: Smart Slider, GSAP, Resource, Adaptive
- **Информация об адаптациях**: история изменений, причины, эффективность
- **Рекомендации**: автоматические предложения по оптимизации

#### Отладка в консоли браузера:
```javascript
// Детальная информация о производительности
window.PerformanceMonitor.debug()

// Статистика адаптаций
window.AdaptiveManager.debug()

// Тестирование ручной адаптации
window.AdaptiveManager.manualAdaptation('fps_drop', 'high')
```

## 🌐 Деплой на продакшн

### Вариант 1: Статический хостинг (Netlify, Vercel, GitHub Pages)

1. **Подготовка файлов**:
   ```bash
   # Убедитесь, что все файлы готовы
   ls -la
   ```

2. **Загрузка на хостинг**:
   - Загрузите все файлы проекта на выбранный хостинг
   - Убедитесь, что `index.html` находится в корневой папке

3. **Настройка API ключа**:
   - Установите переменную окружения `GEMINI_API_KEY` на сервере

### Вариант 2: VPS/Cloud Server

#### Настройка сервера (Ubuntu/Debian):

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip nginx -y

# Установка Node.js (опционально)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### Развертывание проекта:

```bash
# Клонирование проекта
git clone https://github.com/RockInMyHead/nuroweb.git
cd nuroweb

# Установка зависимостей
pip3 install -r requirements.txt

# Настройка переменных окружения
echo "GEMINI_API_KEY=your_api_key" > .env
```

#### Настройка Nginx:

Создайте конфигурацию `/etc/nginx/sites-available/neuroevent`:

```nginx
server {
    listen 80;
    server_name neuroevent.ru;

    # Проксирование на Python приложение
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:
```bash
sudo ln -s /etc/nginx/sites-available/neuroevent /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### Настройка systemd для Neuroevent:

Создайте файл `/etc/systemd/system/neuroevent.service`:

```ini
[Unit]
Description=Neuroevent AI Image Generation Web App
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/nuroweb
ExecStart=/usr/bin/python3 server.py
Restart=always
Environment=GEMINI_API_KEY=your_api_key

[Install]
WantedBy=multi-user.target
```

Запустите сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable neuroevent
sudo systemctl start neuroevent
```

### Вариант 3: Docker

#### Dockerfile:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["python", "server.py"]
```

#### docker-compose.yml:

```yaml
version: '3.8'

services:
  neuroevent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./generated_images:/app/generated_images
```

Запуск:
```bash
docker-compose up -d
```

## 🔧 Конфигурация

### Настройка API ключей

1. Получите API ключ от Google Gemini
2. Добавьте его в файл `.env`:
   ```env
   GEMINI_API_KEY=your_actual_api_key
   ```

### Настройка CORS

В файле `server.py` уже настроен CORS для всех доменов. Для продакшна рекомендуется ограничить домены:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://yourdomain.com"],  # Замените на ваш домен
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## 📁 Структура проекта

```
nuroweb/
├── assets/
│   ├── css/
│   ├── js/
│   ├── images/
│   └── vendor/
├── generated_images/
│   └── metadata.json
├── image_service.py       # Сервис генерации изображений (DALL-E 3)
├── server.py             # FastAPI сервер
├── requirements.txt      # Python зависимости
├── env-example.txt       # Шаблон для переменных окружения
├── index.html            # Главная страница
├── demo.html             # Демо страница
├── performance-test.html # Тест производительности
├── .env                  # Переменные окружения (создайте самостоятельно)
└── README.md
```

## 🚨 Устранение неполадок

### Проблема: CORS ошибки
**Решение**: Убедитесь, что сервер запущен на порту 8000 и CORS настроен правильно в server.py.

### Проблема: Изображения не генерируются
**Решение**: Проверьте API ключ OpenAI в файле `.env`. Убедитесь, что у вас есть достаточный баланс на аккаунте OpenAI. Проверьте логи сервера на наличие ошибок API.

### Проблема: "Rate limit exceeded"
**Решение**: DALL-E 3 имеет строгие ограничения по количеству запросов. Подождите некоторое время перед повторными попытками или обновите тарифный план OpenAI.

### Проблема: "Content policy violation"
**Решение**: DALL-E 3 имеет строгие правила контента. Измените промпт, чтобы он не содержал запрещенных тем или материалов.

### Проблема: Медленные анимации
**Решение**: Проект автоматически определяет производительность устройства и адаптирует анимации. Проверьте страницу `/performance-test.html` для диагностики уровня производительности.

### Проблема: Система оптимизации не работает
**Решение**: Убедитесь, что файл `performance-manager.js` загружается перед `plugins.js`. Проверьте консоль браузера на ошибки инициализации PerformanceManager.

### Проблема: Неправильный уровень производительности
**Решение**: Система оценки может быть неточной для некоторых устройств. Вы можете вручную установить уровень производительности:

```javascript
// В консоли браузера
window.PerformanceManager.updatePerformanceTier('low'); // или 'medium', 'high'
location.reload(); // Перезагрузка страницы для применения настроек
```

## 📞 Поддержка

Если у вас возникли проблемы с деплоем или настройкой проекта, создайте issue в репозитории GitHub.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

---

**Автор**: RockInMyHead  
**Репозиторий**: https://github.com/RockInMyHead/nuroweb.git


